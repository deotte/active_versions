  <div class="w-full lg:max-w-prose border-2 border-solid border-slate-300 rounded-lg items-center">
    <h2 class="text-xl mt-4 mb-4 px-4">E-Commerce Settings</h2>
    <hr class="w-full">
    <div class="mt-4 flex justify-between items-center px-4">
      <h3 class="font-light text-slate-700">Primary Contact</h3>
      <span><%= customer.primary_contact&.truncate(27) %></span>
    </div>
    <div class="mt-4 flex items-center justify-between px-4">
      <span class="font-light text-slate-700">Language</span>
      <% language_text = customer.language.empty? ? "Not set" : customer.language.capitalize %>
      <span><%= language_text %></span>
    </div>
    <div class="mt-4 flex justify-between items-center px-4">
      <h3 class="font-light text-slate-700">Promo Code</h3>
      <% promo_text = (customer.promo_code.nil? || customer.promo_code.empty?) ? "Not set" : customer.promo_code %>
      <span><%= promo_text %></span>
    </div>
    <div class="mt-4 flex justify-between items-center px-4">
      <h3 class="font-light text-slate-700">Support Level</h3>
      <% support_text = customer.support_level.empty? ? "Not set" : customer.support_level %>
      <span><%= support_text.capitalize %></span>
    </div>
    <div class="mt-4 flex justify-between items-center px-4">
      <h3 class="font-light text-slate-700">Customer Rating</h3>
      <% rating_text = customer.rating.empty? ? "Not set" : customer.rating.upcase %>
      <span><%= rating_text %></span>
    </div>
    <hr class="w-full mt-4">
    <div class="flex justify-end items-center p-4">
      <h3>Contact Support</h3>
      <span class="material-icons-round text-blue-400 ml-2 hover:cursor-pointer hover:shadow-lg">mail</span>
    </div>
  </div>