<% if current_page?(path) %>
  <%= link_to "#{text}", path, class: "w-full p-4 bg-blue-400 text-white flex items-center justify-center font-semibold" %>
<% else %>
  <%= link_to "#{text}", path, class: "w-full p-4 flex items-center justify-center h-full" %>
<% end %>