<% if current_page?(path) %>
  <%= link_to "#{text}", path, class: "py-2 px-4 rounded-md bg-blue-400 text-white flex items-center justify-center font-semibold" %>
<% else %>
  <%= link_to "#{text}", path, class: "py-2 px-4 flex items-center justify-center h-full" %>
<% end %>